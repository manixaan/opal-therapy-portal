Migrations are append-only and checksummed — editing an applied file makes the
runner refuse to start. Add the next `NNN_*.sql`; never modify an existing one.

Read `.claude/rules/database-migrations.md`; full detail in `README.md` here.
