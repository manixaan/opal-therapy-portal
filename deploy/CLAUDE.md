Read `.claude/rules/infrastructure.md`.

Editing a script here is not permission to run it. No deployment, no `az`
mutation, no workflow trigger outside an explicitly authorised RELEASE task.
`staging-synthetic.local.txt` is a local credential file — never read or commit it.
