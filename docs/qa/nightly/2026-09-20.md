# Nightly tracker audit — 2026-09-20

## 1. Status line

**RAN** · 2026-09-20T18:45Z UTC · commit `a6da9327` (origin/develop) · this is a **Sunday UTC
weekly deep run** — the full unit and integration suites were also run once, on top of every
feature's targeted checks. 9 commits landed since the last audit (2026-09-19T18:24Z UTC, commit
`cec0a039`), split between finishing three FCA layout fixes and a further round of Opal
Assist/AI-gateway work (warm-up, plain-text formatting, an AI status page, Word/Excel document
tools).

Counts across the tracker's 13 features: **broken 0 · proven 1 · tab-unproven 7 ·
needs-refinement 1 · built-untested 1 · untouched 3**. Identical to last night's counts — every one
of the 13 features was re-verified fresh tonight (every listed test re-run on its own database, not
carried forward) and none of tonight's 9 commits changed any feature's evidence label.

The weekly full-suite run found **one failing test**, `tests/integration/readonly-and-hardening.itest.js`
— see item 5 below. It is not attributed to any tracker feature as `broken`: it is this sandbox's
own limitation (the test process runs as `root`, which bypasses the Unix permission bits the test
uses to simulate a storage write failure), not an application defect. Full detail in Technical
detail.

## 2. Do this first

1. **The tracker itself is still stuck on "idea" for all 13 cards, five audits running — and Opal
   Assist has grown again with still no card.** Tonight added 5 more Opal Assist/AI commits on top
   of the 11 flagged two nights ago: background warm-up (nobody meets a cold start), plain-text-only
   replies (no markdown notation from Opa or Opal Assist), an owner-only AI status page (last 15
   outcomes, metadata only), and Word/Excel/Outlook document tools plus a WA suburb list for Assist.
   Independently re-verified tonight (not delegated): `node --check` clean on all 12 touched
   backend files; 635/635 unit tests across 24 suites including the AI-gateway boundary guards;
   24/24 integration tests (`assist.itest.js`, `opa.itest.js`) against a real database with all 70
   migrations applying cleanly; the new AI status endpoint confirmed to return metadata only
   (feature, status, deny_reason, provider, latency_ms — no content) behind
   `requireAuth`+`requireRole('owner','admin')`; the warm-up module confirmed to go through the
   normal gateway (`ai-gateway.generate()`), not a direct vendor call. This is now a 16-commit,
   CRITICAL-classification body of work, correctly gated and fully tested, still completely
   invisible to the Development Manager.
2. **Report Templates — three more FCA layout commits landed tonight, better tested than the three
   from before but still no browser proof.** `4eadba8`, `35a3d08`, `c4bd811` (Contents starts on its
   own page, white label/value tables, no empty page 2) all shipped with matching structural test
   coverage this time (unlike the three from three nights ago, `05b4376`/`c09883c`/`c4f124b`, which
   still have zero test coverage). But none of the six commits' visual claims — does the cover's
   36-blank-line spacing really fit exactly, do the white labels read correctly, does the header/
   footer/logo geometry hold — has ever been checked by anything other than the author's own eyes in
   Word. `docs/qa/BROWSER_QA_RESULTS.md` has no FCA entry and hasn't been touched since 2026-08-23.
3. **Portal Onboarding Workflow and Xero Integration Works both remain fully built, fully green, and
   fully unproven in a browser under their current tab names.** Onboarding: 573 unit + 176
   integration, unchanged four audits running, no browser check has ever exercised the
   package-defaults view or payroll-status screen. Xero/Finance: 70 unit + 39 integration, unchanged;
   `docs/qa/BROWSER_QA_RESULTS.md` flow H predates the Finance tab and only proves the older
   Accounting tab.
4. **Portal-Splose-Outlook's Settings UI is still unproven, and tonight's one relevant commit doesn't
   close the gap.** `ff19cab` (the all-day-row calendar fix) does touch Outlook-synced calendar
   chips, but it's layout/badge rendering only — no sync, credential, or "Multi Calendar Rules"
   checklist item changed. The practitioner-linking/API-key Settings screen added several nights ago
   still has zero browser proof (160 unit + 21 integration, all green).
5. **Weekly full-suite finding: `readonly-and-hardening.itest.js` fails in this sandbox because the
   test runs as root.** D-7 simulates an unwritable storage directory with `fs.chmodSync(dir, 0o000)`
   and expects the next write to fail with 5xx; on Linux, `root` bypasses directory permission bits,
   so the write silently succeeds (confirmed by direct reproduction: `chmod 000` on a scratch
   directory still let a root-owned process write into it). This is unrelated to any commit in the
   window (the test file was last touched 2026-08-23) and does not indicate the application itself
   is broken — it indicates the test's failure-simulation technique doesn't hold under a root test
   runner. Worth a fix to the test (e.g. drop privileges, or simulate the failure by mocking `fs`
   instead of relying on OS permission enforcement) so this suite is trustworthy in any sandboxed CI
   that also runs as root.

## 3. Every feature

| Feature | Tracker stage | Evidence label | Reason | Tests run |
|---|---|---|---|---|
| Portal Onboarding Workflow | idea | tab-unproven | mature 11-route subsystem, fully green, no browser proof of package-defaults/payroll screens | 573 unit ✓, 176 integration ✓ |
| Xero Integration Works | idea | tab-unproven | Finance tab well tested, zero browser proof of Dashboard/Payroll/Invoicing | 70 unit ✓, 39 integration ✓ |
| Portal - Splose - Outlook \| Integration Works | idea | tab-unproven | sync engine strong and green; tonight's calendar fix is layout-only, doesn't touch Settings UI gap | 160 unit ✓, 21 integration ✓ |
| Automated Reminders | idea | untouched | "Therapist Snapshot" real and tested but answers a different question; zero code for client-facing reminders | 0 unit, 6 integration ✓ (Snapshot only) |
| Opa Mobile Companion | idea | built-untested | guard is self-scoped-by-design; Case Noting's SQL still has zero real-database proof | 41 unit ✓, 0 integration |
| Inductions | idea | needs-refinement | 2 of 3 tracker tasks built and heavily tested, all 3 tasks still `todo`; stale `tutorials.spec.js` assertion confirmed still stale | 366 unit ✓, 130 integration ✓ |
| Report Templates | idea | tab-unproven | 3 new commits well-tested structurally, but 6 layout commits total still lack any browser/rendered proof | 569 unit ✓, 120 integration ✓ |
| Interactive Assessments | idea | tab-unproven | core untouched again tonight; no browser proof of the assessment page | 481 unit ✓, 81 integration ✓ |
| Clinical resources | idea | proven | untouched again tonight; browser QA now 50 days stale but content still valid | 370 unit ✓ (12 skipped), 133 integration ✓ |
| Professional Development | idea | tab-unproven | two disconnected implementations, both tested at API level; existing browser QA only thinly touches the My Profile half | 33 + 36 + 42 integration/unit ✓ |
| Employee Personal Page (My Profile Tab) | idea | tab-unproven | ambiguous target unchanged; both candidate screens guarded and tested, no browser proof | 42 + 82 guard tests ✓ |
| Password Master Document | idea | untouched | not a code feature — a OneDrive document | none |
| Update Opal Docs Register | idea | untouched | not a code feature — a spreadsheet | none |

**Weekly full-suite run** (Sunday UTC): unit `npm test` — **140 suites, 4492/4504 passed, 12
intentionally skipped, 0 failed**. Integration `npm run test:integration` — **61 suites, 990/991
passed, 1 failed** (`readonly-and-hardening.itest.js`, sandbox-root artifact, see item 5 above and
Technical detail).

## 4. Prompts for the morning

**1 — Tracker stage sweep, plus a card for Opal Assist (disagreement, not a code task)**
```
No Claude Code session needed. In the Development Manager, move Portal Onboarding Workflow, Xero
Integration Works, Portal-Splose-Outlook Integration, Report Templates, Interactive Assessments,
Clinical resources and Professional Development off "idea" — all seven are built, guarded and
passing every targeted test, five audits running. Also add a tracker card for "Opal Assist" (the
practice-wide AI assistant with Word/Excel/Outlook task panes, warm-up, plain-text replies, an
owner AI status page, and a de-identification engine covering every clinician-facing AI feature) —
it is now a 16-commit, CRITICAL-risk body of work with no card at all, three audits running.
```

**2 — Report Templates: verify six FCA layout changes by opening a real document (CRITICAL)**
```
/opal-critical
Six commits have changed the FCA Word master's visual layout with no verified rendered result:
05b4376 (footer table geometry), c09883c (logo in the running header band), c4f124b
(Anti-Bribery-and-Corruption-Standard sizing), 4eadba8 (Contents starts on its own page via 36
blank lines on the cover), 35a3d08 (white label/value gallery tables), c4bd811 (no empty page 2,
label text survives an Update Contents). Generate one real FCA report through the wizard
(#fca-root, backend/fca-routes.js) and open it in Word. Confirm: logo placement, footer geometry,
font sizing, that the cover's blank lines land Contents on its own page with no stray blank page,
and that the label/value tables read with white text correctly.
fca-preview-pages.test.js deliberately disables header rendering (renderHeaders: false; jsdom can't
read the logo's Blob URL), so this can only be checked by hand or a real-browser/PDF-render check.
Done when: a human or automated visual check confirms all six changes render correctly, recorded in
docs/qa/BROWSER_QA_RESULTS.md or a new e2e/tests/*.spec.js.
```

**3 — Portal Onboarding Workflow: prove the flow in a browser**
```
/opal-feature
573 unit + 176 integration tests pass, four audits running, with zero code changes since. Walk
through Edit Onboarding's package defaults view and the Payroll Setup screen (all rows ready →
status reaches "Ready for review") as an admin, using a scratch employee record. Record it as a new
docs/qa/BROWSER_QA_RESULTS.md entry or an e2e/tests/*.spec.js.
Done when: a fresh browser/E2E proof exists for both screens.
```

**4 — Portal-Splose-Outlook: browser-prove the Settings UI**
```
/opal-feature
Self-service Splose-practitioner linking and practice-wide API-key management live under Settings →
Integrations → Splose (mockup_v3.html). It's guarded and tested at the API level (160 unit, 21
integration, all green) but nobody has opened this screen in a browser. Walk through: an owner
connecting/disconnecting a practice API key, and a therapist linking their own Splose practitioner
identity.
Done when: a fresh docs/qa/BROWSER_QA_RESULTS.md entry or E2E spec covers both flows.
```

**5 — Fix `readonly-and-hardening.itest.js`'s root-runner assumption**
```
/opal-fast-change
backend/tests/integration/readonly-and-hardening.itest.js's D-6/D-7 test simulates an unwritable
storage directory with fs.chmodSync(dir, 0o000) and expects the next document upload to fail with
5xx. Under a root test runner (this nightly audit's sandbox, and possibly some CI images), root
bypasses directory permission bits, so the write silently succeeds and the assertion fails —
demonstrated by chmod 000 on a scratch directory still allowing a root process to write into it.
This is a test-technique gap, not an application bug. Make the failure simulation not depend on
being a non-root process — e.g. mock fs.writeFile/fs.promises.writeFile to reject for this one
case, or check process.getuid?.() === 0 and skip with a clear message on a root runner.
Done when: the test passes under both a normal user and a root test runner, or clearly skips with
an explanatory message under root rather than silently reporting a false failure.
```

## 5. Ideas without a start

None created or substantively rewritten tonight beyond the mismatch corrections below — every one
of the 13 folders already existed from last night and every evidence label held, so every other
START.md's guidance stays valid.

- `report-templates/START.md` — rewritten to add tonight's 3 new FCA commits to the "Start here"
  list (now six commits to verify visually instead of three), since the underlying gap it describes
  (no rendered/browser proof) is still open and the new commits belong in the same check.

## 6. What changed since last audit

Since the last audit (2026-09-19T18:24Z UTC, commit `cec0a039`), 9 commits touched `backend/` or
`frontend/current/`. Grouped by subsystem:

- **AI / Opal Assist** (5 commits: `a6da932`, `bec9e92`, `c8c59d8`, `8f54b82`, `618b67a`) → **no
  tracker feature.** A "Open in Opal Assist" button on the chat bubble; background warm-up for the
  AI directory, fingerprints and model path (keep-warm every 10–15 min, one silent transport retry,
  never a retried policy/guardrail denial); plain-text-only replies for Opa and Opal Assist (no
  markdown notation); an owner-only AI status page listing the last 15 interaction outcomes
  (feature/status/deny-reason/latency, no content); and Assist's Word-pane "Opal format"/"say it in
  your own words" tools, Excel sheet tools, and a WA suburb list for de-identification. Independently
  verified tonight: `node --check` clean on all 12 touched files; 635/635 unit across 24 suites
  (including `ai-gateway-boundary.test.js`/`ai-single-gateway-guards.test.js`); 24/24 integration
  (`assist.itest.js`, `opa.itest.js`) with all 70 migrations applying cleanly; `ai-policy.js` still
  declares `deidentification` for `clinical_note_generation`, `opa_assistant` and `opal_assist`; the
  new status endpoint returns metadata only, guarded `requireAuth`+`requireRole('owner','admin')`;
  the warm-up module calls through `ai-gateway.generate()`, never a raw vendor SDK. No TODO/FIXME
  found. See item 1 in "Do this first."
- **Report Templates (FCA)** (3 commits: `c4bd811`, `35a3d08`, `4eadba8`) → **tracked, `tab-unproven`,
  unchanged label.** See that feature's STATUS.md and item 2 in "Do this first."
- **Calendar** (1 commit: `ff19cab`) → touches Portal-Splose-Outlook's territory (Outlook-synced
  all-day chip rendering) but is layout-only; does not close that feature's Settings-UI gap. See
  that feature's STATUS.md.

**No subsystem changed without a matching tracker feature, except Opal Assist/AI — already flagged
for three audits running, now with a further 5 commits added tonight.**

## 7. Technical detail

**Setup**
- PostgreSQL was down at session start (`service postgresql status` → `down`); started with
  `service postgresql start`, confirmed `online`. `backend/node_modules` was already installed
  (`ls backend/node_modules/.bin/jest` present); `npm ci`/`npm install` were not run.
- Tracker fetch succeeded on the first attempt:
  `NODE_USE_ENV_PROXY=1 SUPABASE_URL="$SUPABASE_URL" node ../scripts/nightly-audit/fetch-tracker.mjs
  /tmp/tracker.json` → 13 features, 19 tasks.
- Last-audit baseline read from `origin/claude/nightly-audit:docs/qa/nightly/2026-09-19.md` and its
  `features/` folder (this branch is force-pushed fresh from `develop` every run, so
  `docs/qa/nightly/` on `develop` never accumulates dated briefs — the branch's own history was
  copied forward into this session's working tree first: `git checkout origin/claude/nightly-audit
  -- docs/qa/nightly/`). Change window computed as `git log cec0a039..origin/develop -- backend
  frontend/current` (9 commits), matching the prior brief's documented cutoff commit `cec0a039`.

**Work split:** the AI-gateway/Opal-Assist verification (5 commits), the Report Templates FCA
review (3 commits), and the three quick untouched features (Automated Reminders, Password Master
Document, Update Opal Docs Register) were run directly; the remaining eight features were split
across four parallel background agents (Onboarding+Xero; Splose-Outlook+Inductions;
Assessments+Professional-Development+Clinical-resources; Mobile-Companion+Profile-page), each
independently re-locating code, re-checking guards, and re-running every targeted test fresh
tonight rather than trusting last night's numbers. Each pass used its own dedicated database name
(`therapy_scheduler_n5a`/`n5b`/`n5c`/`n5d`, plus `n5main` for this session's own direct integration
runs) to avoid cross-session database contention, following the concurrency pattern from prior
audits — none occurred tonight.

**Commands run** — all from `backend/`. Targeted, per-feature: unit ≈ 2,935 tests passed (12
intentionally skipped), 0 failed; integration ≈ 730 tests passed, 0 failed, across all 13
features' own dedicated test files (full per-file breakdown is in each feature's STATUS.md).
**Weekly deep run** (today is Sunday UTC): `npm test` from `backend/` — **140 suites, 4492/4504
passed, 12 skipped, 0 failed, 42.05s**. `DB_NAME=therapy_scheduler_audit DB_PASSWORD=audit npm run
test:integration` — **61 suites, 990/991 passed, 1 failed, 829.9s**:
`tests/integration/readonly-and-hardening.itest.js` — `D-6/D-7: storage failure behaviour (local
backend) › missing storage object → 404; write failure → 5xx with NO orphaned row`, expected
`fail.status >= 500`, got `201`. Root-caused directly: this session's test process runs as `root`
(`whoami` → `root`); the test's D-7 half relies on `fs.chmodSync(dir, 0o000)` making the directory
unwritable, but root bypasses Unix DAC permission checks. Reproduced in isolation outside the test
suite (`chmod 000` on a scratch directory, then a write into it as root still succeeded and the
content read back correctly) — confirming this is a sandbox/runner artifact, not a code regression.
The test file itself was last touched 2026-08-23, well outside this window, and no commit in
tonight's 9-commit window touches document-storage code. Not counted toward any feature's `broken`
classification. See item 5 in "Do this first" for a proposed fix.

**Correction made to a prior audit's artifact:** none found needed tonight (last audit's
`portal-splose-outlook-integration-works/START.md` mismatch was already corrected as of last
night).

**Could not locate** (all re-checked fresh tonight, all still absent)
- "Induction Playground" (Inductions task) — repo-wide case-insensitive grep, no match.
- "Assessment Review" (Interactive Assessments task) — no literal match.
- "Client Agreement Form" (Report Templates task) — the codebase still calls this "Service
  Agreement."
- "Automated Client Appt Reminders" (Automated Reminders' own stated idea) — still nothing in the
  codebase sends a client-facing appointment reminder; checked tonight's 9 commits too.
- A single, unambiguous target for "Employee Personal Page (My Profile Tab)" — still two real
  candidate surfaces (the People register and the My Profile tab).
- "Multi Calendar Rules" (Portal-Splose-Outlook task) — no code, table, or UI string matches; the
  all-day-row calendar fix tonight is layout-only and doesn't implement any of its checklist items.

**Open questions for the team**
- Whether "Opal Assist" should get a tracker card of its own now, given its size (16 commits) and
  CRITICAL classification — repeated and sharpened from prior audits.
- Whether the induction assistant's direct-vendor-API exception (`ab55bbc`) should get an entry in
  whatever document tracks approved AI-governance exceptions.
- Repeated from prior audits: whether `requireAuth`-only (self-scoped, no `requirePermission` tier)
  is meant to stay the accepted pattern for personal-data routes (`snapshot-routes.js`,
  `mobile-routes.js`, `onboarding-employee-routes.js`'s `/me/*` group) — this audit continues to
  treat it as an accepted exception per the rubric, but it has never been confirmed as a deliberate
  team decision.
- New tonight: should the integration test suite generally assume it may run as `root` in some CI
  or sandbox environments? `readonly-and-hardening.itest.js` is the only test found relying on OS
  permission bits to simulate failure, but the same technique could exist elsewhere undetected until
  a Sunday run surfaces it.

**Tracker write-back**: `post-tracker.mjs` reported `13 features posted, 0 already posted tonight,
brief posted` on the first attempt — every feature's `claude_update`/`next_action`/activity row and
the workspace brief summary went in cleanly.
