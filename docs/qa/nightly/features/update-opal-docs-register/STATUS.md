# Update Opal Docs Register

- Tracker stage: idea
- Tracker environment: none
- Evidence label: **untouched**
- Addressed in this change window (2026-09-24 → 2026-09-25): no — zero commits landed in this window at all
- Created (any located code): no

## Located files

None. This is an organising job (updating an existing Excel document
register and linking files in SharePoint), not a coding task — there is
nothing to find in this repository, and nothing should be expected here.

## Tests

None applicable.

## Open tasks from the tracker

- "Update Excel Sheet to include all documentation required" — status `todo`.
  Notes describe reviewing the existing Excel register, adding tracking
  columns, and linking/attaching documents in the practice's existing
  SharePoint.

## Commits in the window that touched it

None (nothing in `backend`/`frontend/current` could touch this — it lives
outside the codebase).

## Disagreement

None. Tracker stage (`idea`) matches the evidence (`untouched`).
