# Nightly tracker audit — 2026-09-22

## 1. Status line

**RAN.** 2026-09-22, ~18:05–18:45 UTC. Commit audited: `74601fc` (branch `develop`). Last audit:
2026-09-21, commit `17627ce` (see `docs/qa/nightly/2026-09-21.md`, on this branch only — `develop`
never carries prior audit briefs forward, so this run fetched `origin/claude/nightly-audit` directly
for the correct "since last audit" window, per that brief's own §7 correction).

**18 commits** landed since last audit, split three commits touching test files only (bringing tests
up to date with renamed anchors/routes) and 16 feature commits. Every one of the 13 tracker features
was re-verified fresh tonight (re-located, re-guard-checked, targeted tests re-run on a session-unique
database) — three had real, substantive changes; the other ten were confirmed unchanged with all
previously-passing tests re-run and still passing.

**Counts:** 1 proven · 8 tab-unproven · 2 needs-refinement · 2 untouched · 0 built-untested ·
0 broken (13 features total) — the same label distribution as last night, but do not read that as "a
quiet night": one feature closed a real, previously-flagged gap (Portal Onboarding Workflow's dynamic
contract), and two gained new, currently-unproven surface area (Portal-Splose-Outlook, Opa Mobile
Companion) that a label-only diff would miss entirely.

## 2. Do this first

1. **Opal Assist — still no tracker card, fifth consecutive night, still growing.** Four more commits
   landed tonight (`16bc60d`, `15b4176`, `ab95b69`, `8786675` — Format check, a Word pane rebuilt
   around FCA shortcuts, margins/orientation/header controls, cursor/table/style tools). The AI
   gateway boundary and policy registration are both confirmed clean, and all its own tests pass
   (19/19 `assist-page.test.js`, 291/291 the wider `ai-*` governance group) — this is not a code-safety
   finding, it's a visibility one. The surface is now roughly 3,000 lines of CRITICAL-classification
   (AI, de-identification-adjacent) code with zero tracker card and zero browser/E2E coverage.
2. **Case Notes tab — a second, untested path into clinical AI generation.** Tonight's `1c17e78`
   built a genuine desktop case-note composer (`frontend/current/casenotes-compose.js`) with its own
   on-device dictation, directly in the portal — not just a review surface for mobile-dictated drafts
   any more. It's well-guarded and unit/integration tested (412 + 6 tests passing), but it reaches the
   same Bedrock-backed clinical-summary path the mobile pilot does, and that path already had zero
   end-to-end proof. There are now two live, unproven entry points instead of one.
3. **Calendar/Book tab — Splose practitioner linking, now two unproven UI surfaces.** Tonight's
   `37f4e057` ("Users & Roles redesigned") added a *second* Splose-linking screen — an owner assigning
   a practitioner to someone else, from Settings → Users & Roles — alongside the self-service one
   that's been flagged unproven since 2026-09-18. Both are correctly guarded and have route-level
   test coverage, but neither has ever been driven by a browser test or E2E spec. Five consecutive
   audit nights on the original gap; it just got wider instead of closing.
4. **Onboarding tab — real progress, one decision needed next.** Good news first: tonight's `7e3f921`
   + `28ab180` build the dynamic employee Contract of Employment that last night's brief flagged as
   the biggest gap — it now reads offer terms (remuneration, position, dates) the same way the Letter
   of Offer does, and is tested (102 new unit tests, integration passing). What's left is smaller:
   SharePoint document storage still doesn't exist anywhere in the code (only a CSP allowlist entry),
   and Stage 1's "Send" button is still an Outlook draft, not a real send. Both need a decision from
   Anthony, not more speculative code.
5. **Inductions — the same stale E2E assertion, still unfixed.** `e2e/tests/tutorials.spec.js:59`
   still asserts a heading ("All learning") that no longer exists in `resourcehub.js`. This has now
   been flagged for multiple consecutive nights; it's a one-line fix.

## 3. Every feature

| Feature | Tracker stage | Evidence label | Reason | Tests run |
|---|---|---|---|---|
| Xero Integration Works | idea | tab-unproven | Finance tab built + tested, no browser/E2E proof — unchanged | 70 unit + 31 integration ✓ |
| Update Opal Docs Register | idea | untouched | Non-coding task, nothing in repo — unchanged | none |
| Report Templates | idea | tab-unproven | FCA/letter/template engine built + tested, no browser/E2E proof — unchanged | 1050 unit + 201 integration ✓ (shared) |
| Professional Development | idea | tab-unproven | Split across Resource Hub PD + My Profile CPD, no tracker tasks — unchanged | 345 unit ✓ (shared), 133 integration ✓ (shared) |
| Portal - Splose - Outlook | idea | tab-unproven | Sync engine proven; **new tonight**: a 2nd unproven Splose-linking UI (Users & Roles) alongside the original | 388+23 unit + 91+2 integration ✓ |
| Portal Onboarding Workflow | idea | needs-refinement | **Dynamic contract now built + tested tonight**; SharePoint storage & Stage 1 "Send" still gaps | 597/598 unit* + 138 integration ✓ |
| Password Master Document | idea | untouched | Non-coding task, nothing in repo — unchanged | none |
| Opa Mobile Companion | idea | tab-unproven | **New tonight**: desktop case-note composer with on-device dictation, well-tested, zero E2E | 412 unit + 6 integration ✓ |
| Interactive Assessments | idea | tab-unproven | WHODAS fully built + tested, no browser/E2E proof — unchanged | 1050 unit + 201 integration ✓ (shared) |
| Inductions | idea | needs-refinement | Stale `tutorials.spec.js:59` assertion still unfixed; tonight only a test-anchor update | 639/12 unit + 128 integration ✓ |
| Employee Personal Page (My Profile Tab) | idea | tab-unproven | Built + mostly tested, "24hr" reminder is actually weekly-Friday — unchanged | 345 unit ✓ (shared), 56/57** integration ✓ |
| Clinical resources | idea | proven | No distinct code — maps entirely to the (proven) Resource Hub — unchanged | 639/12 unit ✓ (shared), 133 integration ✓ |
| Automated Reminders | idea | tab-unproven | Snapshot built + tested, client reminders not built at all — unchanged | 6 integration ✓ (Snapshot only) |

\* One test in `onboarding-document-reader.test.js` failed
("the OCR language data is shipped with the portal") — confirmed an environment artifact: the
`@tesseract.js-data/eng` package is declared in `backend/package.json` but genuinely missing from
`backend/node_modules` in this sandbox, and this audit cannot run `npm install` to fix that. Not a
code defect. See §7.

\*\* Same known `readonly-and-hardening.itest.js` sandbox-root `chmod` artifact flagged every prior
audit night — root bypasses the simulated storage-write failure, not a code defect. See §7.

## 4. Prompts for the morning

**`/opal-critical` — Opal Assist, close the tracker visibility gap**
> Read `docs/qa/nightly/features/README.md`'s note on Opal Assist and
> `backend/ai/ai-policy.js`'s `opal_assist` entry for what already exists. This is CRITICAL work
> (AI, de-identification-adjacent) with no Development Manager feature card after five straight nights
> of active growth (`backend/assist-routes.js`, `backend/assist/`, `frontend/current/assist*.js/.css`,
> ~3,000 lines). Before more code lands here: create a tracker feature card for it (or confirm with
> Anthony it's intentionally out-of-tracker), and add at least one E2E/browser QA entry — there is
> currently zero browser coverage of any kind.

**`/opal-critical` — Opa Mobile Companion, prove the new desktop composer end to end**
> Read `docs/qa/nightly/features/opa-mobile-companion/START.md` for full context.
> `frontend/current/casenotes-compose.js` (new tonight) lets a therapist dictate and generate a case
> note directly in the portal, reusing the mobile pilot's Bedrock-backed `/api/mobile/case-note-drafts/*`
> endpoints. Add `e2e/tests/case-notes-compose.spec.js`, following `portal.spec.js`'s login/tab
> pattern: log in as a therapist, open Case Notes → "New case note," pick a caseload client, type a
> transcript (dictation isn't reliably automatable in CI), complete the names check, generate a draft,
> and assert it lands in the review surface linked to the right client. Also fix
> `docs/mobile/CASE_NOTE_AI_PRIVACY.md` — it still says this feature needs `ANTHROPIC_API_KEY`; the
> code (both the mobile pilot and this new composer) routes exclusively through AWS Bedrock.

**`/opal-feature` — Portal-Splose-Outlook, prove both Splose-linking UIs**
> Read `docs/qa/nightly/features/portal-splose-outlook-integration-works/START.md`. Extend
> `e2e/tests/portal.spec.js` with a describe block that: opens Settings → Users & Roles
> (`frontend/current/people.js`, new tonight), exercises the owner-assigns-practitioner dropdown for a
> seeded user (success + already-claimed-practitioner-disabled cases), and separately exercises the
> pre-existing self-service Settings → Integrations → Splose screen as both an owner and a therapist.
> One spec covering both closes a gap that's been open since 2026-09-18.

**`/opal-critical` — Portal Onboarding Workflow, decide SharePoint and Stage 1 Send**
> Read `docs/qa/nightly/features/portal-onboarding-workflow/START.md`. The dynamic Contract of
> Employment is now built and tested (`backend/onboarding-contract-docx.js`,
> `backend/onboarding-document-reader.js`) — no code needed there. What's left needs a decision, not a
> build: is SharePoint storage for onboarding documents still wanted, given documents already persist
> reliably in Postgres today? And is Stage 1's Outlook-draft "Send" (confirmed intentional and QA'd)
> still the final design, or is a real send path still wanted? Record both decisions in the tracker
> (feature `32b768b1-8bf3-40a6-9669-a8908922aa21`) before any further code lands on either.

**`/opal-fast-change` — Inductions, fix the stale e2e assertion**
> `e2e/tests/tutorials.spec.js:59` asserts
> `page.getByRole('heading', { name: 'All learning' })` — that heading text no longer exists in
> `frontend/current/resourcehub.js`; the Owner's Assign Learning view now shows
> `<h1 class="rh2-h1">Assign Learning</h1>` with three tabs. Replace the assertion with the current
> heading text and confirm the spec passes.

## 5. Ideas without a start

No feature folder was newly created tonight — all 13 already existed from prior nights. Every
`STATUS.md` was rewritten fresh; `START.md` was substantively rewritten for the three features whose
underlying gap changed (Portal Onboarding Workflow — the old "build the dynamic contract" prompt is
now obsolete and replaced with the SharePoint decision; Portal-Splose-Outlook — now covers two linking
UIs instead of one; Opa Mobile Companion — now covers the new desktop composer) and left otherwise
intact where the gap it describes is unchanged.

## 6. What changed since last audit

Since the last audit (2026-09-21, commit `17627ce`), **18 commits** touched `backend`/`frontend/current`
or their tests:

- **Onboarding** (9 commits, `9982ef2` through `4141680`) — the dynamic Contract of Employment
  (`7e3f921`, `28ab180`), a start-date timezone fix, a returned-document rendering fix, and UI
  simplification of the cancellation flow. Maps to **Portal Onboarding Workflow**.
- **Users & Roles** (`37f4e057`, plus a follow-up chevron fix `2c23476` and two test-anchor updates
  `7b55537`/`74601fc`) — a redesigned admin page including a new owner-assigns-Splose-practitioner
  flow. Maps to **Portal-Splose-Outlook** (new surface) and touches **Employee Personal Page**'s
  adjacent `employees.js` only by proximity, not by code change.
- **Case notes** (`1c17e78`) — a new desktop dictation composer. Maps to **Opa Mobile Companion**.
- **Opal Assist** (4 commits, `16bc60d` through `8786675`) — Format check and Word-pane feature growth.
  **No tracker feature maps to this** — flagged for a fifth consecutive night, see §2.

No other subsystem changed without a matching tracker feature this window.

## 7. Technical detail

**Setup**: PostgreSQL was already running; `backend/node_modules/.bin/jest` was present, so no
`npm install` was needed. Tracker fetch: `fetch-tracker.mjs` returned 13 features, 19 tasks.

**Window correction applied from the start** (learned from 2026-09-21's own §7): this session fetched
`origin/claude/nightly-audit` directly rather than trusting `develop`'s empty `docs/qa/nightly/`, found
the four prior dated briefs (09-18 through 09-21), and used 2026-09-21 → 2026-09-22 as the correct
window from the outset.

**Parallel investigation**: given the size of tonight's window (18 commits touching 4 distinct
subsystems), this audit delegated deep re-verification of the four most-changed areas (Portal
Onboarding Workflow, Portal-Splose-Outlook, Opa Mobile Companion, Opal Assist) to parallel sub-agent
investigations, each independently reading diffs, checking guards, and running its own targeted
tests; findings were cross-checked against this session's own reading before being written into the
feature folders. The remaining nine features were re-verified directly by this session by re-running
each of last night's recorded test commands and confirming identical results.

**Commands run** (all from `backend/`, targeted per feature, never a complete suite — Tuesday UTC,
not Sunday, so no weekly deep run):

```
npx jest tests/finance-flags.test.js tests/finance-routes.test.js tests/xero-payroll-*.test.js
npx jest tests/fca-*.test.js tests/letter-*.test.js tests/templates-*.test.js tests/assessment-*.test.js tests/whodas-*.test.js
npx jest tests/pd-catalogue-guards.test.js tests/frontend-stage2-guards.test.js tests/frontend-stage3-guards.test.js tests/credential-surface-guards.test.js tests/security.test.js
npx jest tests/induction-*.test.js tests/learning-*.test.js tests/walkthrough-content.test.js tests/assign-learning-guards.test.js tests/ai-direct-provider.test.js tests/resource-*.test.js
npx jest tests/onboarding-*.test.js (24 files)
npx jest tests/splose-*.test.js tests/sync*.test.js tests/outlook-*.test.js tests/scheduler-*.test.js tests/travel-*.test.js tests/availability-engine.test.js tests/contact-matching.test.js
npx jest tests/casenotes-compose.test.js tests/case-note-routes.test.js tests/casenotes-helpers.test.js tests/clinical-note-provider.test.js tests/clinical-note-deidentify.test.js tests/mobile-routes.test.js
npx jest tests/assist-page.test.js tests/ai-*.test.js

DB_NAME=<session-unique> DB_PASSWORD=audit npx jest --config jest.integration.config.js <matching .itest.js files, per group above> --runInBand
```

**Concurrency note**: this audit's own parallel sub-agents (and this session's sequential runs)
repeatedly collided on the default `therapy_scheduler_audit_test` database name, producing transient
`deadlock detected` / connection-refused / unexpected-status failures on first attempts in three
separate groups (Xero's `finance-routes.itest.js`, Onboarding's integration group, Opa Mobile
Companion's integration group, Portal-Splose-Outlook's `admin-people.itest.js`). Every one was
confirmed via `pg_stat_activity` or a clean re-run under a session-unique `DB_NAME` to be a shared-
database collision, per `.claude/rules/tests.md`'s own documented concurrency warning — not a code
defect. All re-runs are what's reported as authoritative above.

**Totals** (approximate — some unit test files are shared across more than one group above and are
not double-counted where known, but the induction/learning/resource group and the onboarding/splose/
mobile groups were run by different sub-agents and may overlap slightly on shared guard files):
unit — roughly 100+ suites, several thousand tests, 2 known non-code failures (see below), otherwise
0 failures. Integration — roughly 45 suites, ~790 tests, 0 code-defect failures (several transient
DB-collision failures on first attempts, all resolved on clean re-run — see above).

**The two known non-code failures**:
1. `tests/integration/readonly-and-hardening.itest.js`, "D-6/D-7: storage failure behaviour (local
   backend)" — this container runs as `root`; the test simulates an unwritable storage directory via
   `fs.chmodSync(dir, 0o000)`, which root bypasses on Linux, so the simulated failure never occurs.
   Flagged by name in every prior audit night since 2026-09-20; still unfixed (report-only, `backend/
   tests/**` edits aren't in scope for a nightly audit).
2. `tests/onboarding-document-reader.test.js`, "the OCR language data is shipped with the portal" —
   the `@tesseract.js-data/eng` package is declared in `backend/package.json:28` but missing from
   `backend/node_modules` in this sandbox (`ls backend/node_modules/@tesseract.js-data` → No such
   file or directory). This audit independently confirmed both the declared dependency and the
   missing directory. Cannot fix without `npm install`, which this audit's hard limits prohibit. New
   tonight (the test itself is new, added alongside `onboarding-document-reader.js`'s contract-reading
   work) — worth confirming this package installs cleanly in CI/staging, where it presumably does.

**Not located / open questions** (carried forward from prior nights, still true):
- `backend/onboarding-policies/` (named in this audit's own setup instructions) does not exist; the
  closest match is `backend/onboarding-templates/`.
- `backend/service-agreement-routes.js` (named in this audit's own setup instructions) does not
  exist; the Service Agreement is served by `backend/templates-routes.js` +
  `backend/templates/service-agreement-map.js`.
- `backend/routes-outlook-integration.js` and `backend/routes-backup-original.js` remain dead/
  unmounted duplicate Outlook/sync logic, not cleaned up (report-only).
- `backend/instrument-register-routes.js` still has no test file matched by name.
- `onboarding-workflow-routes.js` and `onboarding-assignment-routes.js` still both expose
  `/api/onboarding/assignments*` — overlap not confirmed either way.
- Whether `ENABLE_WHODAS_ASSESSMENT` and `CLINICAL_NOTE_AI_ENABLED` are actually `true` in the live/
  staging environment was not checked (out of scope for a code-only audit).

**Write-back**: `post-tracker.mjs /tmp/writeback.json` → `post-tracker: 13 features posted, 0 already
posted tonight, brief posted`. Ran successfully, before the git push below, per this audit's own
ordering rule.
